package com.examquizai.backend.controller;

import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Exam session lifecycle endpoints (start, attempt, submit).
 *
 * <p><b>Placeholder for future implementation.</b> Not implemented yet.</p>
 */
@RestController
@RequestMapping("/api/v1/exams")
@Tag(name = "Exams", description = "Not yet implemented")
public class ExamController {
    // TODO: implement exam lifecycle endpoints in a future iteration
}
